# module.py

Example module demonstrating proper exception handling (explicit exception
types, log-before-handle, re-raise unexpected). Referenced by CodeBot's own
audit tickets as a style example. Not imported at runtime.

## Key Exports
- `process_data()`: Function
- `safe_operation()`: Function
